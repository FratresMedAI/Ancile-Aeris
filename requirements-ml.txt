# Optional ML / vision stack for CLEARSKY_SIM_MODE=false
# Install into the ClearSky venv or host Python used by the nodes:
#   pip install -r requirements-ml.txt
ultralytics>=8.3.0
opencv-python-headless>=4.8.0
numpy>=1.24.0
torch>=2.1.0
