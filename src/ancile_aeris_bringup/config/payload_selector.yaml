# Ancile Aeris - Property of Fratres X AI
# Simulation-safe payload selector for demo and roadmap capabilities.

demo_profile: basic

features:
  # Baseline demo path keeps copilot and scout mesh enabled.
  enable_copilot: true
  enable_scout_mothership: true

  # Explicit nested controls consumed by ancile_aeris_basic_demo.launch.py.
  mesh_motherships:
    enabled: true
    count: 2

  baby_interceptor:
    enabled: false
    require_double_authorization: true

  # Non-kinetic effector stack (HPM-style denial, cognitive jamming/spoofing,
  # multi-sensor deception, authorized control-link takeover).
  effectors:
    enabled: true
    enable_sim: true

  # Thin cognitive chain (agent_orchestrator -> digital_twin -> cognitive_ew)
  # used by ancile_aeris_basic_demo.launch.py to demonstrate effector-aware
  # selection. Independent of the full bringup.launch.py feature flags below.
  cognitive_demo_chain:
    enabled: true

  # Buildable local packages that are outside the basic demo slice.
  enable_agent_orchestrator: false
  enable_adversarial_defense: false
  enable_causal_xai: false
  enable_cognitive_ew: false
  enable_continual_learning: false
  enable_defensive_swarm_coordinator: false
  enable_digital_twin: false
  enable_federated_learning: false
  enable_generative_red_team: false
  enable_hyperspectral_stub: false
  enable_neuromorphic_sim: false
  enable_resilient_pnt: false
  enable_swarm_orchestrator: false
  enable_verification: false
  enable_video_analytics: false
  enable_zero_knowledge_sharing: false
