# Ancile Aeris — Property of Fratres X AI
# Optional simulation interceptor configuration.

/**:
  ros__parameters:
    require_simulate_only_deploy_cmd: false
    require_double_authorization: true

/baby_interceptor_node:
  ros__parameters:
    payload_mode: "low_collateral_stub"
    attritable_unit_cost_tier: "low"

