cmake_minimum_required(VERSION 3.16)
project(continual_learning)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/continual_learning_node.py DESTINATION lib/${PROJECT_NAME} RENAME continual_learning_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_continual_learning tests/test_continual_learning.py)
endif()
ament_package()
