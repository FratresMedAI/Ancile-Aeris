cmake_minimum_required(VERSION 3.16)
project(adversarial_defense)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/adversarial_defense_node.py DESTINATION lib/${PROJECT_NAME} RENAME adversarial_defense_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_adversarial_defense tests/test_adversarial_defense.py)
endif()
ament_package()
