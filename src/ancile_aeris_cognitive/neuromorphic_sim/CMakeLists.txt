cmake_minimum_required(VERSION 3.16)
project(neuromorphic_sim)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/neuromorphic_sim_node.py DESTINATION lib/${PROJECT_NAME} RENAME neuromorphic_sim_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_neuromorphic_sim tests/test_neuromorphic_sim.py)
endif()
ament_package()
