cmake_minimum_required(VERSION 3.16)
project(copilot)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/copilot_node.py DESTINATION lib/${PROJECT_NAME} RENAME copilot_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_copilot tests/test_copilot.py)
endif()
ament_package()
