cmake_minimum_required(VERSION 3.16)
project(generative_red_team)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/generative_red_team_node.py DESTINATION lib/${PROJECT_NAME} RENAME generative_red_team_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_generative_red_team tests/test_generative_red_team.py)
endif()
ament_package()
