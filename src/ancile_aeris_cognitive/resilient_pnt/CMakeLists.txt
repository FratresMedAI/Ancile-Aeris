cmake_minimum_required(VERSION 3.16)
project(resilient_pnt)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/resilient_pnt_node.py DESTINATION lib/${PROJECT_NAME} RENAME resilient_pnt_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_resilient_pnt tests/test_resilient_pnt.py)
endif()
ament_package()
