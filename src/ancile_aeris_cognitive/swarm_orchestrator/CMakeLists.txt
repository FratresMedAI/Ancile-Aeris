cmake_minimum_required(VERSION 3.16)
project(swarm_orchestrator)
find_package(ament_cmake REQUIRED)
find_package(ament_cmake_pytest REQUIRED)
install(PROGRAMS nodes/swarm_orchestrator_node.py DESTINATION lib/${PROJECT_NAME} RENAME swarm_orchestrator_node)
install(DIRECTORY launch config DESTINATION share/${PROJECT_NAME})
if(BUILD_TESTING)
  ament_add_pytest_test(test_swarm_orchestrator tests/test_swarm_orchestrator.py)
endif()
ament_package()
