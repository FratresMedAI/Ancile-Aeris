apiVersion: v1
kind: Service
metadata:
  name: clearsky-os-dashboard
  namespace: clearsky-os
spec:
  type: NodePort
  selector:
    app: clearsky-os-core
  ports:
    - name: streamlit
      port: 8501
      targetPort: 8501
      nodePort: 30501
